// The production facility will also create portable movie players.
// The main difference between these and the audio players is that
// they contain screens. Create an enum called MonitorType that will
// store
public enum MonitorType {
   LCD,
   LED
   ;
}
